import 'package:flutter/material.dart';

abstract class BaseSpacing {
  static const double iconSize = 24.0;
  static const EdgeInsets screenPadding = EdgeInsets.symmetric(horizontal: 16.0);
  static const EdgeInsets rowPadding = EdgeInsets.symmetric(vertical: 8.0);
  static const SizedBox sectionSpacing = SizedBox(height: 24.0);
  static const SizedBox elementSpacing = SizedBox(height: 12.0);
}